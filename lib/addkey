name=$1
key=$2
libdir=$(dirname $0)
keydir=$(dirname ${libdir})/keys
echo type encrypt pass:
read -s pass
echo $key | openssl enc -e -aes-256-cbc -k ${pass} -md sha256 -out ${keydir}/${name}

# Send encrypted file to remote host if configured
/bin/bash ${libdir}/remote_save "${keydir}/${name}"
