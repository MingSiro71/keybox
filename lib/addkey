if [ $# -ne 2 ];then
    echo 'keybox: requires "key name" and "key string" to add.'
    exit 1
fi
name=$1
key=$2
keydir=`dirname $(dirname $0)`/keys/
echo type encrypt pass:
read -s pass
echo $key | openssl enc -e -des -k ${pass} > ${keydir}${name}
